{description, "A basic EUnit test file for an OTP application, to be executed at the top-level, inside."}.
{variables, [
    {topic, "foo", "The topic or concern of a given test file"}
]}.

{dir, "test"}.
{template, "eunit.erl", "test/{{topic}}_test.erl"}.
